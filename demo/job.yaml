apiVersion: batch/v1
kind: Job
metadata:
  name: job-1
spec:
  template:
    spec:
      containers:
        - image: docker.io/dotnetroundhouse/roundhouse
          name: job
          args: 
            - --servername=host.minikube.internal
            - --database=test
            - --silent
            - --databasetype=postgres
            - --donotcreatedatabase
            - --warnononetimescriptchanges
      restartPolicy: Never
      serviceAccountName: default
      shareProcessNamespace: true
